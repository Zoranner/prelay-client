{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Prelay desktop window capability.",
  "windows": ["main"],
  "permissions": ["core:default", "autostart:default"]
}
